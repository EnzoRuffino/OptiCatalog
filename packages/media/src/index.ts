/**
 * Traitement média (alt, compression, IA image) — réservé pour une phase ultérieure.
 */
export type MediaPlaceholder = { note: string };

export const mediaModulePlaceholder: MediaPlaceholder = {
  note: 'Package @opticatalog/media : brancher sharp / pipeline plus tard.',
};
