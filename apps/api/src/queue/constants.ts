/** Nom de file BullMQ partagé entre l’API et le worker. */
export const OPTIMIZE_PRODUCT_QUEUE = 'optimize-product';

/** Optimisation des pages catégorie / collection (PLP). */
export const OPTIMIZE_COLLECTION_QUEUE = 'optimize-collection';
